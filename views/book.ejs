<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>
        <%= book.title %> | Book Notes
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/styles/style.css" />
</head>

<body class="bg-dark text-light">

    <div class="container py-5">
        <!-- Back Link -->
        <div class="mb-4">
            <a href="/" class="btn btn-outline-light btn-sm">&larr; Back to All Books</a>
        </div>

        <!-- Title and Author -->
        <div class="text-center mb-5">
            <h1 class="display-5 text-success fw-bold">
                <%= book.title %>
            </h1>
            <h4 class="text-muted fst-italic">by <%= book.author %>
            </h4>
        </div>

        <!-- Cover and Info -->
        <div class="row justify-content-center align-items-start mb-5">
            <div class="col-md-4 text-center mb-4 mb-md-0">
                <img src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-L.jpg?default=false"
                    class="img-fluid rounded shadow-sm" alt="Cover for <%= book.title %>"
                    onerror="this.onerror=null; this.src='/images/default-cover.jpg';" />
                <div class="mt-3">
                    <span class="badge bg-success fs-6">Rating: <%= book.rating %>/10</span>
                </div>
                <div class="mt-2">
                    <a href="https://openlibrary.org/isbn/<%= book.isbn %>" target="_blank"
                        class="btn btn-outline-info btn-sm">View on Open Library</a>
                </div>
            </div>

            <!-- Notes -->
            <div class="col-md-8">
                <div class="bg-secondary p-4 rounded shadow-sm">
                    <h5 class="text-light mb-3">My Notes</h5>
                    <!-- <p class="text-white-50" style="line-height: 1.8; text-align: justify; white-space: pre-line;">
            book.notes
          </p> -->
                    <div class="mt-4">
                        <%- book.notes %> <!-- Unescaped HTML rendering -->
                    </div>

                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>