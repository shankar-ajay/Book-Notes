<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Edit <%= book.title %> | Ajay Shankar's Book Notes</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/styles/edit.css" />
</head>

<body class="bg-dark text-light">
  <div class="container py-5">
    <h1 class="text-center text-warning mb-4">Edit Book Review</h1>

    <form action="/book/<%= book.id %>/update" method="POST" class="bg-secondary p-4 rounded shadow">
      <div class="mb-3">
        <label for="title" class="form-label text-light">Title</label>
        <input type="text" class="form-control" id="title" name="title" value="<%= book.title %>" required />
      </div>

      <div class="mb-3">
        <label for="author" class="form-label text-light">Author</label>
        <input type="text" class="form-control" id="author" name="author" value="<%= book.author %>" required />
      </div>

      <div class="mb-3">
        <label for="rating" class="form-label text-light">Rating (out of 10)</label>
        <input type="number" class="form-control" id="rating" name="rating" value="<%= book.rating %>" min="1" max="10" required />
      </div>

      <div class="mb-3">
        <label for="date_read" class="form-label text-light">Date Read</label>
        <input type="date" class="form-control" id="date_read" name="date_read" value="<%= book.date_read.toISOString().split('T')[0] %>" required />
      </div>

      <div class="mb-3">
        <label for="isbn" class="form-label text-light">ISBN (optional)</label>
        <input type="text" class="form-control" id="isbn" name="isbn" value="<%= book.isbn || '' %>" />
      </div>

      <div class="mb-4">
        <label for="notes" class="form-label text-light">Notes (Markdown supported)</label>
        <textarea class="form-control" id="notes" name="notes" rows="10" required><%= book.notes %></textarea>
      </div>

      <div class="d-flex justify-content-between">
        <a href="/book/<%= book.id %>" class="btn btn-outline-light">Cancel</a>
        <button type="submit" class="btn btn-warning">Update Book</button>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
